# CLAUDE.md — 12규칙 템플릿

**최우선 진실의 원천 (Single Source of Truth).** 이 파일은 이 저장소에서 수행되는 모든 Claude Code 동작의 최상위 규범이다.

- 조회(Read / Grep / Glob / 조회성 Bash)와 쓰기(Write / Edit / 변경성 Bash / 커밋) 전부 이 규칙에 기반해 동작한다.
- 다른 무엇과 충돌하든 이 파일이 이긴다 — README, `../CLAUDE.md`(모노레포 우산), 코드 주석, 기존 관행, 이전 대화의 합의, 도구의 기본 동작.
- 무효화는 사용자의 명시적 지시로만 가능하다. 무효화된 규칙 번호와 이유를 반드시 말한다(규칙 12).
- 작업 착수 전 해당 작업에 걸리는 규칙을 확인하고, 위반 소지가 있으면 진행 전에 표면화한다(규칙 1·10).
- 강제 수단: `.claude/settings.json` 의 PreToolUse 훅(`.claude/hooks/claude-md-rules.sh`)이 쓰기 도구 실행 직전에 핵심 규칙을 주입한다. 훅 확인·해제는 `/hooks`.

이 규칙은 명시적으로 무효화하지 않는 한 이 프로젝트의 모든 작업에 적용됩니다.
원칙: **사소하지 않은 작업에서는 속도보다 신중함. 사소한 작업은 판단에 맡김.**

## 규칙 1 — 코딩 전에 생각하기
가정을 명시적으로 말한다. 불확실하면 추측하지 말고 물어본다. 모호하면 여러 해석을 제시한다. 더 간단한 접근이 있으면 반박한다. 혼란스러우면 멈추고 무엇이 불분명한지 말한다.

## 규칙 2 — 단순함 우선
문제를 해결하는 최소한의 코드. 추측성 코드 금지. 요청에 없는 기능 추가 금지. 일회용 코드에 추상화 금지. 시니어 엔지니어가 과하다고 할 만하면 단순화.

## 규칙 3 — 외과수술 같은 변경
필요한 곳만 건드린다. 본인이 만든 흔적만 정리한다. 주변 코드, 주석, 포매팅을 "개선"하지 않는다. 망가지지 않은 것을 리팩토링하지 않는다. 기존 스타일에 맞춘다.

## 규칙 4 — 목표 기반 실행
성공 기준을 정의하고, 검증될 때까지 반복한다. 단계를 따르지 말고, 성공을 정의하고 반복한다.

## 규칙 5 — 모델은 판단이 필요한 일에만
사용: 분류, 초안 작성, 요약, 추출. 사용 금지: 라우팅, 재시도, 결정론적 변환. 코드가 답할 수 있으면 코드가 답한다.

## 규칙 6 — 토큰 예산은 권고가 아님
작업당 4,000 토큰. 세션당 30,000 토큰. 예산에 근접하면 요약하고 새로 시작. 초과를 표면화한다. 조용히 넘어가지 않는다.

## 규칙 7 — 충돌은 평균 내지 말고 표면화
두 패턴이 충돌하면 하나를 고른다 (더 최신 / 더 검증된 쪽). 이유를 설명하고 다른 한쪽은 정리 대상으로 표시. 충돌하는 패턴을 섞지 않는다.

## 규칙 8 — 쓰기 전에 읽기
코드 추가 전 export, 직접 호출자, 공통 유틸을 읽는다. "직교로 보입니다"는 위험하다. 구조가 왜 그런지 모르면 묻는다.

## 규칙 9 — 테스트는 의도를 검증
테스트는 "왜 중요한가"를 코딩한다, "무엇을 하는가"가 아니라. 비즈니스 로직이 바뀌어도 통과하는 테스트는 잘못된 테스트.

## 규칙 10 — 중요한 단계마다 체크포인트
각 단계 완료 후 무엇을 했고, 검증됐고, 남았는지 요약. 설명할 수 없는 상태에서 계속 진행하지 않는다. 헤매면 멈추고 다시 정리.

## 규칙 11 — 코드베이스 컨벤션이 취향을 이긴다
코드베이스 안에서는 일관성 > 취향. 컨벤션이 해롭다고 생각하면 표면화한다. 조용히 분기하지 않는다.

## 규칙 12 — 큰 소리로 실패
"완료"는 무엇이라도 조용히 누락됐으면 틀린 말. "테스트 통과"는 하나라도 스킵됐으면 틀린 말. 불확실성을 숨기지 말고 표면화한다.

## 규칙 13 — 객체지향 생활 체조 9가지 원칙
1. 한 메서드에 오직 한 단계의 들여쓰기(indent)만 한다.
2. `else` 예약어를 쓰지 않는다.
3. 모든 원시값과 문자열을 포장한다.
4. 한 줄에 점을 하나만 찍는다.
5. 줄여쓰지 않는다(축약 금지).
6. 모든 엔티티를 작게 유지한다.
7. 3개 이상의 인스턴스 변수를 가진 클래스를 쓰지 않는다.
8. 일급 콜렉션을 쓴다.
9. 게터/세터/프로퍼티를 쓰지 않는다.

---

# 프로젝트 추가 규칙 (13규칙에 종속)

> 이 저장소는 **한↔일 실시간 통역기의 TypeScript + DDD(헥사고날) 재설계**다.
> 도메인: 방(Room)·발화(Utterance)·자막(Subtitle). 외부 세계: Soniox WS, sherpa-onnx,
> LibreTranslate, Google TTS, 브라우저 WebSocket. 참조 구현(Python)은
> `~/Project/speetch-translator` — 동작의 진실이 필요할 때만 읽고, 구조는 베끼지 않는다.

## 규칙 14 — 의존 방향은 협상 대상이 아님

| 불변식 | 내용 |
|---|---|
| 방향은 한쪽으로만 | `domain/` 은 application·adapters·Node API(ws, fetch, fs)를 모른다. 역방향 import 금지 |
| domain 은 순수 TS | 도메인 파일의 `import` 대상은 domain 내부뿐. 외부 패키지가 나타나면 그 파일은 domain 이 아니다 |
| ports 는 인터페이스만 | `SpeechRecognizer` / `Translator` / `SpeechSynthesizer` / `SubtitleNotifier` — 구현·분기·재시도 로직을 두지 않는다 |
| adapters 만 외부를 안다 | Soniox WS·sherpa-onnx·LibreTranslate·Google TTS 는 어댑터 안에서만 등장한다. 엔진 선택 분기를 application 에 두지 않는다 — 조립은 composition root(main) 소관 |
| 애그리게이트 경유 | `Room` 루트를 거치지 않고 참가자를 직접 다루지 않는다. `Utterance` 상태 전이(partial→final)는 Utterance 메서드로만 |
| VO 는 불변 | `Language`·`TranslationMode`·`Speaker`·`Subtitle` 은 값 객체. 생성 후 변경 없음 |
| 도메인 서비스는 무상태 | 턴 판정(TurnPolicy: 침묵 0.9초)·언어 라우팅은 입력→출력. 필드에 상태를 두지 않는다 |
| 도메인 이벤트가 유일한 출구 | `PartialTranscriptProduced`·`SpeakerChanged`·`UtteranceFinalized`·`UtteranceTranslated` — WS 메시지는 이벤트의 직렬화일 뿐, 도메인이 메시지 포맷을 알지 않는다 |

## 규칙 15 — 주석은 «없는 것»이 기본
주석은 리뷰 편의성만을 위해 존재한다. 코드가 말하는 것을 다시 적지 않는다. 뻔한 동작 설명은 과감히 삭제하고, 개발자가 놓치기 쉬운 비즈니스 로직의 의도나 제약 조건만 단어·단문으로 남긴다.

```ts
// 나쁨 — 지우고 다시 써라
// 발화를 확정한다
/** @param audio 오디오 청크 */

// 좋음
// 화자 «전환 직전»에만 플러시한다 — 먼저 플러시하면 새 화자 첫 토큰이 이전 문장에 붙는다
// LID 는 발화당 1회 — 재판정하면 문장 중간에 언어가 바뀌어 자막이 두 동강 난다
```

TSDoc 도 같다. 시그니처가 이미 말하는 것을 `@param`·`@returns` 로 반복하지 않는다. 서브에이전트·워크플로에 일을 시킬 때도 이 규범을 프롬프트에 넣는다 — 안 넣으면 그쪽에서 주석이 쏟아진다.

## 규칙 16 — 이름은 1초 만에 읽혀야 한다
AI 가 자동 생성한 듯한 상투적이고 장황한 문장형 테스트명을 완전히 배제한다.

```ts
// 나쁨
it("should finalize utterance when silence exceeds threshold and speaker did not change", ...)

// 좋음 (vitest)
it("침묵 0.9초 → 발화 확정", ...)
it("화자 전환 → 이전 발화 먼저 플러시", ...)
it("만석 방 입장 → 거절", ...)
```

한 테스트는 한 가지를 검증한다. 이름에 「그리고」가 두 번 나오면 테스트를 쪼갠다.

## 규칙 17 — 메시지 계약은 «추가만», 선언은 코드가 아니다
이 프로젝트의 스키마는 DB 가 아니라 **WS 메시지 계약**이다 (기존 Python 백엔드·배포된 프론트와 공유).

- 메시지 타입·필드는 **추가만** 한다. 기존 필드의 이름 변경·의미 변경·삭제 금지. 새 필드는 optional 로 붙인다 (`speaker?: number` 가 선례).
- 새 메시지 타입을 만들기 전에 반드시 먼저 말한다 — 기존 타입 확장으로 되는지부터 검토.
- 「온보딩 때 정하고 가끔 고치는」 선언(엔진 선택, 모델 경로, 서비스 URL, VAD 파라미터)은 env·설정 파일로 두고 코드에 박지 않는다. 두 벌이 되면 한쪽만 낡는다.
